{
  "name": "edev",
  "version": "1.0.0",
  "description": "Run a series of scripts based on a configuration file and pipe their outputs to the console.",
  "main": "./bin/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "edev": "./bin/index.js"
  },
  "preferGlobal": true,
  "author": "Vlladin",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.1"
  }
}
